package = pelib
version = 1
minor = 0
release = 0

prefix = $(HOME)/.local/debug
exec_prefix = $(prefix)
bindir = $(exec_prefix)/bin
libdir = $(exec_prefix)/lib
includedir = $(prefix)/include

CONFIG = DEBUG

subdirs = src include

## Targets to run before any other target runs
## Can be a custom target
## To target by default
FIRST=
